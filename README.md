# TaskIT

A frontend task management application that allows selecting users and managing their individual task lists. Tasks are stored in the browser's localStorage, ensuring persistence across sessions.

## Features

- User selection
- User-specific task rendering
- Add new tasks
- Complete and remove tasks
- Persistent storage using browser localStorage
